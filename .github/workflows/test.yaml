---
name: test
on:
  pull_request:
  push:
    branches:
    - gh-readonly-queue/**
    - master
jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go:
        - "1.22"
        - "1.21"
        - "1.20"
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-go@v7
      with:
        go-version: ${{ matrix.go }}
    - run: go test -v ./...
